# 08 — 受控 SSH 逐步执行（fake）与 Redis 计划互斥

**What to build:** 操作计划经人审批准后，仅能由控制面经受控 SSH 执行端口在图内物理主机上逐步执行：生产适配 MINA SSHD，验收主路径使用 recording fake（不算第二条验收接缝）。多副本下用 Redis 分布式锁保证同一活跃计划互斥执行；步骤失败或受阻立即停止并作废计划，禁止改步重试或旁路直连续跑。用于真实 SSH 的主机凭证从本票起按加密存储落地（fake 模式可不落密）。禁止 Agent/UI 旁路直连主机。

**Blocked by:** 07 — 「修实际回 A」选支与操作计划人审

**Status:** done

- [x] 批准后的计划可经控制面执行端口逐步推进；CI/验收默认走 SSH fake 并留下可断言的调用记录
- [x] 未批准计划不能执行
- [x] 执行目标限于图内物理主机，拒绝随意 off-graph 宿主
- [x] 多副本设计下同一活跃计划执行临界区有 Redis 锁互斥
- [x] 步骤失败/受阻 → 立即停止且计划作废；不能原地改步重试
- [x] 真实 SSH 路径的凭证加密存储已就绪（fake 可不使用明文密债）
