# 01 — 临时身份头与高级/一般角色门禁

**What to build:** 从空脚手架（仅 health）往上长：请求携带临时身份头即可被控制面解析为平台用户，并带有高级/一般（或 ADMIN/OPERATOR 等价）角色；无身份或越权调用受保护能力时被拒绝。为后续冲突协作门禁提供可验证的身份基础，不引入完备 JWT，不复活已删旧认证模块语义。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] 带合法临时身份头的请求可解析出稳定用户身份与角色（高级/一般）
- [x] 缺少身份头或映射失败时，受保护 API 拒绝访问（统一 API 信封可断言）
- [x] 角色差异可在至少一个受保护探针/门禁上区分（为后续协作规则铺路）
- [x] 不引入 JWT 完备方案；不复活旧安全模块语义
