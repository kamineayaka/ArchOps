package com.archops.common.api;

/**
 * Discriminated HTTP result of {@code POST /api/conflicts/{id}/branch-selection}.
 * JSON stays the selected record shape so FIX_ACTUAL clients keep reading plan fields.
 */
public interface BranchSelectionResult {
}
