# ArchOps

运维关系真相系统：以关系真相为主生命线，但产品成立需要三件套——关系真相、连接工作台、AI 诊断（含人审执行）。缺工作台或不能 AI 诊断（仅人看冲突图）均不算已成立。指标类可观测可后置。AI 自我迭代为 Later，但禁区与能力/权力分界已定：迭代只提升能力，不自动扩大权力。关系真相采用双轨（策展 / 观测）：策展是运维要达到的目的，观测是了解现场的方式之一；冲突是理想与实际的偏差。在技术与经济可行时，观测全流程拥有最高优先。v1 用一张统一图表达多种对象与关系，分层只是视图。

**合同状态**：领域合同已冻结（见 ADR-0039）。本文件与有效 ADR 为实现依据；改语义须先立新 ADR。

**Agent 入口**：编码助手（含 Cursor Cloud Agent）必须先读根目录 `AGENTS.md` / `CLAUDE.md`，再读本文件与 ADR-0043。

**实现接手**：脚手架已按 ADR-0043 重建。竖切按 `.scratch/vertical-slice-mvp/issues/` 与 `docs/dev-handoff.md` 推进；不复用已删旧域。

**技术栈**：已冻结（ADR-0043 选型会）。Java 21 / Spring Boot 3 / Gradle / MyBatis-Plus；React + Ant Design（同镜像静态）；PostgreSQL（v1 图也落 PG；图库 Later）；Redis（队列/锁/会话/缓存，多副本）；Python Agent（systemd）；MINA SSHD；WebClient AI 出站；交付 `archops:latest` + Postgres + Redis。

## Language

**关系真相**:
运维世界中「存在哪些对象、对象之间是何种关系」这一领域问题的总称。以双轨维护，不假定策展与观测永远一致。是产品主生命线，但是产品成立的三件套之一，而非唯一成立条件。
_Avoid_: 库存、资产清单、CMDB、单纯的拓扑展示（「展示」不等于「真相」）

**产品成立**:
可交付产品被承认「已成立」的条件：同时具备关系真相、连接工作台（含 SSH/连库等动手能力）、AI 诊断与操作建议（含人审后执行）。三者缺一不成立。
_Avoid_: 仅有冲突图、仅有工作台、仅有聊天式 AI

**主责**:
产品主生命线所在：看清并维护关系真相。注意：主责不等于产品成立的全部条件；成立见「产品成立」。
_Avoid_: 把主责与三件套混称、愿景清单

**高级角色**:
可对冲突执行已知悉、指派/自任处理人、在无处理人或处理人拒绝后改派的角色（如主管）。不自动等于冲突处理人。
_Avoid_: 管理员（未定义时勿混用）、任意可见者

**一般角色**:
可认领尚未已知悉的冲突（自行已知悉并成为处理人），可接受/拒绝/转让指派；不能认领已有归属的冲突，也不能在非处理人时开该冲突操作计划。
_Avoid_: 无归属抢单、待接受即执行

**策展真相**:
经人工确认后写入的关系描述，表示运维需要达到并维护的目的状态（理想 / 目标）。人是作者；AI 至多起草，不能独自定稿。业务上有时不能向现场让步。
_Avoid_: 现场事实的唯一断言、文档、注释（除非已确认写入策展）

**观测真相**:
由探测或心跳附带的状态快照写入的现场关系描述，表示实际看起来怎样。是运维了解当前状态的方式之一；写入观测轨不需人审，也不自动覆盖策展真相。仅在观测通道新鲜（心跳未超时）时，观测值可作为「实际」参与比对与规范问法。
_Avoid_: 监控指标、日志、可观测性（那些是信号来源，本身不是关系描述）；对策展的裁判；心跳已超时仍把旧值当可靠实际

**观测空洞**:
策展侧存在某对象或关系事实，但观测侧**无当前可用值**：从未写入，或心跳超时导致原观测不可再用。观测空洞**不是冲突**（冲突要求两侧都有可用值且不等）；它是「无法确认实际」类提醒，但走与冲突同一套协作：已知悉产生归属、冲突处理人等。AI 诊断在纯空洞场景只允许「恢复观测通道/心跳/核验」类分叉，禁止使用过期实际值给出现场唯一落点。若原先已有开放冲突，心跳超时进入空洞时：冲突**挂起**为「因空洞无法确认实际」，不关闭；诊断先只处理恢复观测；恢复并写出新可用观测后，按同一合并键继续或冲突升级，并触发重诊，旧选支与活跃计划作废。空洞期间不得用过期观测值回答「实际在哪」，也不得据此关闭冲突或执行指向过期实际的操作计划。
_Avoid_: 把空洞当成冲突、超时后仍展示旧实际当可信、用空洞假装已对齐、因超时直接关闭原冲突、空洞不走处理人协作、挂起后继续执行指向旧实际的计划

**观测消失**:
心跳正常且状态快照成功，明确断言某策展对象或关系在现场不存在（例如找不到带标签的容器）。这是**可用观测值**（值为不存在），可与策展「应存在 / 应运行于…」构成冲突，不得当成观测空洞。规范问法下「实际在哪 / 是什么」答「不存在 / 未观测到该对象」，并同屏策展值。
_Avoid_: 把明确不存在当成空洞或仅身份失联而不进入冲突比对

**心跳**:
观测端向系统定期汇报存活，并可**附带状态快照**（推送式观测）的机制。快照经身份匹配后写入观测真相并触发与策展的比对。心跳超时 → 相关观测视为观测空洞（旧值不再作为可靠实际）；已有冲突则挂起而非关闭。
_Avoid_: 只探活却假装已更新关系事实；用心跳替代策展；超时后继续沿用旧观测值；超时直接关冲突

**冲突**:
同一条关系或同一个对象事实上，策展真相与**当前可用的**观测真相不一致，即理想与实际的偏差。两侧都必须有可用值才能成立；观测空洞时不产生冲突。按「一条关系 / 一个对象事实」比对。冲突不是「谁对谁错」；观测的主价值是提醒发生了偏差。只要偏差仍存在就必须可被处理；允许已知悉的暂态偏差。渐行渐远含：（G2）自**冲突警告首次发出**起持续超过该事实类型的时间阈值仍未对齐——已知悉不豁免，时钟在空洞挂起期间**继续累计**；（G3）已知悉期间实际偏差扩大（如 B→C）则冲突升级。G2 触发时：打破静音，**仅再提醒冲突处理人与冲突归属方**（不因此自动改派）。冲突关闭的前提是该合并键上策展值等于观测值；已知悉不是关闭。即便已相等，仍须经「已对齐，待确认关闭」由冲突处理人确认后才关闭；未相等时不得关闭。
_Avoid_: 告警（更广的运维事件）；以谁为准；把观测空洞当成冲突；G2 从已知悉/接受起算；空洞期间暂停 G2 时钟；G2 超时自动改派；并行堆叠多条互斥的「新冲突」而不合并演变；人强制关闭仍不等的两轨

**渐行渐远阈值**:
G2 的时间上限按事实类型分设（可配置）。v1 出厂默认：容器 `运行于`（位置）**8h**；网络可达 **72h**；其余关系事实兜底 **24h**。时钟自冲突警告首次发出起算，已知悉不豁免，空洞挂起不暂停。
_Avoid_: 全局单一固定阈值却声称按类型；已知悉豁免超时；无出厂默认

**冲突升级**:
同一策展事实上（合并键 = 同一对象 + 同一关系类型，例如同一容器的 `运行于`），观测实际值发生变化（如理想仍为 A，实际由 B 变为 C）时，不新开第二条并行冲突警告，而是合并并升级既有冲突：当前陈述为「策展 A、实际 C」，并保留演变脉络 A→B→C。每次升级须保留提醒记录以供审计。不得同时存在 AB 与 AC 两条活跃警告。
_Avoid_: 新冲突、重复开单、按业务标签粗暴合并（易误合）、只报最新不留演变、只留历史不报最新

**规范问法**:
涉及双轨对象或关系时的标准提问与回答纪律：「应该在哪 / 是什么」只对应策展；「实际在哪 / 是什么」只对应观测；「是否一致」回答一致或冲突（并给出两轨值）。即使只问「实际」，回答仍须同屏带上策展（P2），不得把单轨说成唯一真相；存在冲突时，不得给出唯一落点的操作指令而不披露偏差与当前实际。
_Avoid_: 「App 在哪」这类未拆开的唯一问句（在冲突存在时不合法）

**已知悉**:
冲突级的全局状态（全员可见）：承认暂态偏差并进入处理流程。点击已知悉的人取得该冲突的**冲突归属**。允许暂无冲突处理人。高级角色已知悉后可指派组内一般角色为处理人，也可自任处理人。一般角色**只能**对尚未已知悉的冲突自行已知悉并同时成为处理人（认领）；不可抢认领已由他人已知悉的冲突。已知悉不消除冲突，也不把任一轨改写成「已对齐」。提醒可暂时静音，但一旦偏差发生变化（含观测实际值改变或超时未对齐），必须重新大声提醒并按冲突升级处理。
_Avoid_: 已裁定、以策展为准、忽略冲突、关闭提醒即等于对齐、把已知悉当成个人私有已读、静音后对恶化不再提醒、一般角色抢认领已已知悉的冲突

**冲突归属**:
因点击已知悉而落到该操作者身上的冲突主责归属（例如主管先已知悉，则归属在主管）。归属与冲突处理人可以分离：归属方可指派他人为处理人。处理人转让只变更处理人，不变更归属。一般角色不能通过认领夺走已有归属的冲突。已有处理人且存在进行中的处理时，归属方不能强行改派；须现任处理人结束工作或自行转让。
_Avoid_: 与处理人混为同一概念、无已知悉却有归属、转让处理人时顺带改归属、归属方强行打断执行中的处理人

**冲突处理人**:
当前唯一负责处理该冲突的人（同一冲突同一时间仅一人）。只有**已接受**的冲突处理人可以生成并执行针对该冲突的操作计划，并在待确认关闭态确认关闭。被高级角色指派后进入「待接受」：在接受之前不能开操作计划；须在**接受 / 拒绝（须说明理由）/ 转让**中选择；转让只转处理人（归属不变），且须经拟接手人同意。拒绝后冲突保持已知悉、暂无处理人，归属不变。一般角色仅可认领尚未已知悉的冲突（自行已知悉并成为已接受的处理人）。
_Avoid_: 任一可见者都能开该冲突的操作计划、待接受即可开计划、把点击已知悉自动当成已接受处理人、多处理人并行、抢认领已有归属的冲突

**指派拒绝**:
一般角色拒绝高级角色将其设为冲突处理人的安排，且必须说明理由。拒绝后该冲突保持已知悉、无处理人；归属仍在原归属方，由其改派他人或自任。不得在无处理人时生成该冲突的操作计划。
_Avoid_: 静默拒绝、拒绝却仍保持为处理人、拒绝后清除已知悉/归属

**待确认关闭**:
该合并键上策展值已等于观测值之后的状态：系统提示「已对齐，待确认关闭」，须**冲突处理人**确认才真正关闭冲突。此态提醒对全员保持可见，不算已知悉静音。未相等不得进入此态，更不得关闭。若在此态期间观测再次漂移导致与策展不等：退出待确认关闭，按同一合并键做冲突升级并再大声提醒。处理人点确认时若瞬间已不相等：确认失败，提示刷新，保持或回到开放冲突，不得关闭。
_Avoid_: 自动关闭、非处理人关单、已知悉静音、强制关闭未对齐冲突、确认时不管是否仍相等仍关闭、另开平行新冲突而不升级

**观测优先**:
在技术与经济可行的前提下，观测端全流程（探测 → 写入观测轨 → 与策展比对 → 发出或升级冲突提醒）相对其他能力拥有最高优先。部分对象类型可降级为手动触发探测，但仍必须走同一套写观测 → 比对 → 升级语义，不得另造「手填就算对齐」的旁路。
_Avoid_: 无条件实时；降级成不比对、不升级；牺牲主责去堆无关功能却饿死观测链路

### 策展写入

**草案**:
对策展真相的拟议变更（增删改），以可审查的 diff 呈现。人发起描述后可由 AI 起草。冲突处理中：仅当所选分支需要改策展时才出现草案并须人审；纯修现场的分支跳过草案。草案在确认前不是策展真相的一部分。策展写入的另一合法路径见操作计划中的「策展对齐步骤」（须对应已审草案内容）。
_Avoid_: 已确认变更、临时真相、待确认策展、纯修现场仍强制走草案

**逐条确认**:
草案内按条（对象或关系）接受或拒绝。接受的条目立即写入策展真相；未接受的仍只是草案。
_Avoid_: 整单只能全有或全无（那是被拒绝的确认单位）

### 观测写入

**探测**:
获取现场证据并写入观测真相的过程。v1 包括：心跳附带状态快照（主路径之一），以及主机/K8s API/数据库元数据等拉取或核验。写入观测轨不需人审。写入前须按对象身份规则匹配；匹配失败则产生未绑定观测候选。心跳超时不写新值，而使相关观测进入观测空洞。
_Avoid_: 策展写入、把探测结果直接当成策展真相、匹配失败仍强行合并、心跳超时后仍保留旧值当可靠实际

**未绑定观测候选**:
探测发现了现场实体，但无法可靠匹配到既有一等对象时的产物。可提醒「发现未映射现场对象」；不自动并入策展对象，也不自动升级既有冲突。要并入必须经人（草案）绑定或新建。
_Avoid_: 自动新建并默认可信的策展对象、静默合并进近似对象

### 对象身份

**对象 ID**:
一等对象的稳定主键。总纲：有外部权威 UID 时以其为主键；否则以策展侧不可变对象 ID 为主键。可变名、IP、运行时 ID 等只作匹配线索，不是「是否同一对象」的定义本身。
_Avoid_: 以显示名、业务名、易变指纹单独定义同一

**物理主机**:
一台可被运维定位的机器（或等价计算节点）。身份以策展 `主机ID` 为主键；观测用管理 IP、SSH host key 等作匹配线索。线索变了不自动变新主机，匹配失败走未绑定观测候选，由人重绑线索。
_Avoid_: 以管理 IP 单独作为主键

**数据库**:
作为运维对象的数据库本身。身份以策展 `数据库ID` 为主键；端点、端口、库名等为匹配线索。迁移连接串不等于自动变成新数据库对象。
_Avoid_: 数据库实例；以端点+库名单独作为主键

**Docker 容器**:
一台主机上的容器运行时实体。身份以策展 `容器ID` 为主键，并要求现场具备不可变对象标签（如 `archops.object_id=<容器ID>`）；探测优先用该标签匹配。Docker 运行时 ID、名称只是线索。允许先写入策展再补标：未打标期间不承诺冲突升级链，位置类问题降级为未绑定或弱匹配提醒。标签缺失或被改掉视为匹配失败 → 未绑定观测候选；原对象标记身份失联，不得按同名把新容器静默当成原对象并升级冲突。删重建且标签仍正确时：新 Docker ID、同名、换宿主仍视为同一对象，位置变化走冲突升级。
_Avoid_: 以 Docker ID 或「名+宿主」单独作为主键；无标签仍承诺升级链；把 Pod 当成容器的同一概念

**身份失联**:
既有对象的匹配线索失效（尤其容器不可变标签缺失或被改），以致探测无法可靠认回该对象。此时不得用同名等弱线索自动续上冲突升级；原冲突可保留为失联态，现场新实体走未绑定观测候选。
_Avoid_: 按显示名自动接龙、把失联当成已对齐或已消失而不提示

**K8s 集群**:
一个 Kubernetes 集群整体。身份以 `kube-system` namespace UID 为主键。同名重建集群若 UID 不同则为新对象，不得因同名自动合并。
_Avoid_: 以集群显示名单独作为主键

**K8s 节点**:
集群中的节点对象。身份以策展 `节点ID` 为主键；「运行于哪台物理主机」与集群内 node name 等为匹配线索。重装/改名后由主机关联或人重绑保持同一节点对象。
_Avoid_: 仅以 node name 作为主键；与物理主机强行永久 1:1 而不建节点对象

### v1 图模型

**对象**:
图上的一等节点。v1 种类仅限：物理主机、数据库、Docker 容器、K8s 集群、K8s 节点。业务名（如「订单服务」）只是对象上的标签或名称，不是一等对象。
_Avoid_: 服务、应用、实例（「数据库实例」）、业务系统（作为节点类型）

**角色**:
K8s 节点相对所属集群的职责分类（口语常叫「主从」，如控制面 / 工人）。角色挂在节点上，不是节点与节点之间的边。
_Avoid_: 主从边、Master→Worker 关系边

**关系**:
图上的一等边。v1 必须能表达：网络可达、运行于、属于。口语中的 K8s「主从」不是边，见「角色」。
_Avoid_: 依赖、调用、部署于、主从（作为边类型）

**网络可达**:
策展真相中的**有向**一等边：表示源物理主机到目标物理主机的运维通道可达，v1 验收含义为源主机可 SSH 到目标主机（主机→主机），**不是**「经堡垒/ArchOps 代连即算可达」。A→B 与 B→A 分开维护、分开冲突。
_Avoid_: 仅 UI 连通、临时隧道、无向混为一谈、把堡垒代连成功写成 Host-A→Host-B 可达

**可达核验**:
对**策展已声明**的网络可达边，由**源主机 Agent** 发起 SSH 探活，将成功/失败写入该有向边的观测真相（可随心跳上报）。未核验过的策展可达边处于观测空洞。仅堡垒/控制面代连成功不得写入机间可达观测。默认不对全图做 N² 探测。
_Avoid_: ICMP 冒充 SSH 语义、中心代探冒充源主机视角、无策展边仍全网扫描式探测

**运维隔离区**:
ArchOps 控制面所处的网络位置：与外界业务网/公网默认隔离，对人与 Agent 按堡垒模式收口；主机向控制面有受控回流以报送心跳与状态快照。AI 调用外部模型须走受控出站白名单（见实现 ADR），不得任意外网。
_Avoid_: 整站塞进单台跳板机当作唯一形态；假定无回流还能推送观测；控制面随意访问公网

**运行于**:
表示「谁在谁上面跑」。v1：Docker 容器运行于物理主机；K8s 节点运行于物理主机；数据库可运行于物理主机，也可运行于 Docker 容器。
_Avoid_: 安装在、部署到、对应（不要与「运行于」并行指同一事实）

**属于**:
表示成员归属。v1：Docker 容器可属于某 K8s 集群；K8s 节点属于某 K8s 集群。
_Avoid_: 包含（方向相反时需统一，不要两套词并行）

### AI 诊断与执行

**AI 诊断**:
产品成立条件之一。冲突警告发出之后异步自动生成诊断结果；诊断不得阻塞或拖延警告发出。诊断结果全员只读可见。冲突升级时自动再生成新诊断，旧诊断标记为过时。任何人可再请求只读的分叉建议，但只有**已接受的冲突处理人**能选择分支，且必须基于**当前未过时的诊断**才能选支。选支后的流水线：若分支要改策展 → AI 生成草案 → 人审草案 → AI 生成操作计划 → 人审计划 → 执行；若分支纯修现场 → 跳过草案，直接生成操作计划再人审后执行。计划失败终止后：旧计划作废；AI 可基于状态分析起草新草案与/或新计划，凡有草案须先人审草案，再生成并人审新计划——禁止改写仍在执行或未终止的已审定计划，禁止无人审自动重试。若在已选支或计划待审/执行中发生冲突升级：选支作废，活跃计划受阻即停取消，须基于新诊断重走流水线。禁止在不披露策展、不提供分支的情况下给出唯一落点操作指令。
_Avoid_: 先诊断再警告、旁观者选支/开计划/执行、无当前诊断就选支、改写未终止的已审定计划、升级后沿用旧选支/旧计划、单轨下令

**诊断资料**:
AI 诊断 v1 **必须**可依赖：策展真相、观测真相（含演变）、冲突记录与协作元数据、相关对象身份与非机密标签，以及为诊断所需的**现场状态读取**（经工作台/Agent/API 读取集群、主机、容器等运维状态，B-live；与已有观测 B-obs 并用）。v1 **禁止**依赖业务库表内容及订单/客户/财务等业务数据。工作台/操作计划历史的规律挖掘、外部指标时序、人粘贴补充等涉及自我迭代或加料的，见自我迭代与 Later。
_Avoid_: 用业务敏感内容做诊断；把现场状态读取当成可写策展/可绕过人审

**现场状态读取**:
诊断或计划失败分析时，AI 在权限与敏感拒绝约束下读取图内对象的现场运维状态（B-live），以便判断原因。读取不是观测真相的自动定稿；观测仍只由心跳快照/核验/探测写入。不得借读取之机拉取业务敏感数据。
_Avoid_: 读状态偷偷写观测或策展；读业务数据

**禁止猜测**:
ArchOps 诊断原则：资料不足时不得编造分叉或假装确定。可先按扩图诊断有限补齐子图；一旦触发扩图终止条件，必须明确输出「诊断失败」及所需材料。自我迭代不得成为猜测的借口。
_Avoid_: 幻觉成图、资料不足仍给唯一落点建议、扩图失败后仍假装可执行、用「模型已迭代」绕过资料不足

**拓扑理解**:
v1 诊断必须具备的是对与界面同源的**图结构**的理解（结构化子图），足以支撑规范问法推理。基于画布截图的像素/多模态视觉理解为 Later 加分，不是 v1 成立条件。v1 验收：仅提供结构化子图即可诊断；不要求再吃截图。
_Avoid_: 把看截图当 v1 必备；无图结构只靠视觉臆测（与禁止猜测冲突）

**诊断子图**:
一次诊断装入的结构化图资料。采用双份并行：策展子图一份、观测子图一份，外加冲突对齐表。默认范围（第一轮）：冲突合并键所涉对象及其一跳邻居。
_Avoid_: 整图无差别塞入；单轨子图冒充双轨；默认就拉连通分量（可能过大）

**扩图诊断**:
第一轮用默认诊断子图；若无法判断，先自问还缺哪些对象或关系，再在**策展图与观测图两侧**查找，找到的并入对应子图（允许一侧找到另一侧没有）。将找到部分与默认部分合并后再次诊断。最多三轮：默认 1 次 + 扩图后再诊最多 2 次。终止条件（任一即停）：（1）自问所缺对象与关系全部未找到；（2）已用满三轮仍需继续扩图；（3）已用满三轮仍无法判断原因。停止后必须返回「诊断失败」并列出所需材料，不得猜测补全。
_Avoid_: 无限扩图、找不到还硬编、默认之外再诊超过 2 次、只搜单轨、失败却不列所需材料

**分叉计划**:
AI 在冲突场景下给出的可选处理路径（各绑定假设）。可作为只读诊断/建议全员查看；将某一分支推进为后续流水线，仅冲突处理人可做。
_Avoid_: 单一「请到某主机执行」而不陈述假设与备选、旁观者选定分支即开跑

**操作计划**:
人审通过后的固定有序步骤清单，用于对物理世界或连接目标执行变更，也可包含显式的策展对齐步骤（须对应已审草案）。针对某冲突：仅已接受处理人在选定分支（并完成必要时的草案人审）后，由 AI 生成计划，再经处理人审查通过后执行；同一冲突同一时间最多一份活跃计划。审定后执行中受阻：立即停止，该计划作废；不得改步、加步、自动重试。其后 AI 可起草新计划（若需改策展则先新人审草案），均须再过人审。
_Avoid_: 改写未终止的已审定计划、静默重试、无草案人审就落实策展变更、非处理人开计划

**策展对齐步骤**:
操作计划中的显式步骤：落实**已审草案**中约定的策展更新（通常将合并键策展值对齐到约定实际值）。禁止无已审草案却用对齐步骤偷写策展；禁止计划成功副作用写策展。
_Avoid_: 计划成功副作用写策展、人选分支瞬间未审草案就写策展

**AI 写入边界**:
探测/心跳可写入观测真相。AI 不得在无人审路径下定稿策展；策展只能经草案逐条确认，或经人审计划中对应已审草案的策展对齐步骤写入。AI 可发起现场状态读取，但读取结果不自动成为观测定稿。
_Avoid_: AI 直接定稿策展；观测再加人审；读状态当写真相

**AI 权力与能力**:
权力：选支、草案/计划人审、执行、关单等仍由人（处理人/归属方等）掌握；AI 不能自动扩大权限、改高危/敏感规则、绕过人审。能力：诊断与计划生成的质量可经自我迭代提升（命令偏好、流程模板等），但不得把能力迭代变成权力扩大。
_Avoid_: 能力与权力混谈、迭代自动提权

**处理归档**:
每一次冲突处理全过程（诊断、选支、草案、计划、执行结果、失败原因等）须归档，供事后总结。
_Avoid_: 无归档的自我迭代

**自我迭代**:
Later 能力：基于处理归档总结规律，提升 AI 生成诊断/计划的策略与模板。① 命令替换类（如 netstat→ss）、② 相对固定流程类 → 允许自动迭代到策略/模板。③ 权限缺失类 → 只总结报告给运维，经人工评判后写入权限白名单，不得自动放权。不做生产沙箱自动 A/B 晋升。自我迭代禁止：自动定稿策展、伪造观测、改写未终止的已审定计划、自动关冲突、自动改高危/敏感规则、自动提权、用迭代借口违反 never guess。
_Avoid_: L2 式生产 A/B 自动晋升；权限类自动放行；迭代改写真相或审批规则

**权限白名单**:
运维人工评判后，明确授予 AI 在诊断/计划生成或受控读取中可使用的权限项。仅来自人批，不来自自我迭代自动写入。
_Avoid_: AI 给自己加白名单

### 连接工作台

**连接工作台**:
产品成立条件之一。运维在连接成功后对目标执行命令的工作界面。连接目标必须是图上已有一等对象；未入图目标不可连。
_Avoid_: 对任意主机/库即兴直连而不入图

**破坏性命令**:
工作台对运维写操作的高危判定对象。v1 以**高危模式名单**判定，入选原则：不可逆丢数据（P1）、集群/控制面级中断（P2）、权限与信任边界扩大（P3，如清空防火墙规则、改信任锚点）→ 命中则取消轻确认，升格与操作计划同级的重审批。单对象可逆中断（P5，如单服务 restart）为常规轻确认。产品内置不可删的基础高危集，管理员可追加；未命中不自动当高危。操作计划整单人审已是重审批，计划内步骤不再按破坏性二次分级。例：`docker rm`、`kubectl delete pod`、`iptables -F` 为高危；`systemctl restart nginx`、`docker restart` 为常规。
_Avoid_: 效果制模糊分类作 v1 主策略；计划内二次破坏性分级；把图结构变更当成工作台常规/高危可直接执行

**敏感读拒绝**:
对**业务数据**（客户/订单/财务等）的高风险读取，以及对策展业务库内容的破坏性写（如业务库 `DELETE`/`DROP`）→ 工作台**一律拒绝**，不是审批放行。与运维主机上的 OS 凭证/私钥等运维资料分列：后者 v1 不按业务敏感读拒绝（可走常规轻确认），拒绝桶优先于高危桶（同时命中时拒绝胜出）。
_Avoid_: 业务敏感读审批放行；把业务库 DROP 当成仅重审批；把读 shadow/私钥与读订单表当成同一拒绝理由而不加区分

**图结构变更**:
会改变关系真相所关心的现场结构的操作（如迁移容器宿主、改机间可达所依赖的网络定位）。**必须**走冲突/诊断的操作计划路径；工作台不得以轻确认或高危重审批直接执行这类变更（应拒绝并导流到计划）。
_Avoid_: 工作台直接改图结构；改完不触发执行后探测

**工作台处置三档**:
工作台命令只有三档处置：**拒绝**、**高危（重审批）**、**常规（轻确认）**。图结构变更不是第四档处置，而是强制路由到操作计划。
_Avoid_: 增加「警告但仍轻确认」等含糊档位

**工作台轻确认**:
人已打开的工作台会话中，对日常探查/临时命令，AI 建议可经一次轻确认后执行。工作台与操作计划路径并列：诊断类变更必须走操作计划；工作台不得用于执行「未完成操作计划」的剩余步骤，也不得在计划失败后用轻确认改步绕过冻结纪律。命中高危名单则不得轻确认。非冲突处理人仍可对图内对象做与该冲突无关的轻确认探查，但不能启动针对该冲突的操作计划。
_Avoid_: 无确认执行、用工作台续跑/改写失败的操作计划、把轻确认与操作计划混成一条路径

**执行后探测**:
工作台上成功的结构性变更（如迁移容器）应触发针对相关对象的一次探测，以刷新观测真相并进入比对/冲突升级链路。执行本身不是观测写入，探测才是。
_Avoid_: 把命令输出直接当成观测真相；变更后不刷新观测
