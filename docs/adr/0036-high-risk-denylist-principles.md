# 高危名单原则：P1–P3 高危；业务数据拒绝；图结构走计划

工作台仅三档：拒绝 / 高危重审批 / 常规轻确认。高危入选原则为不可逆丢数据、集群控制面级中断、权限与信任边界扩大（P1–P3）；单对象可逆中断（P5）常规。业务客户/订单/财务等敏感读，以及业务库 DELETE/DROP，一律拒绝（拒绝优先于高危）；修正早先「业务库 DROP=重审批」。OS 凭证/私钥等运维资料不按业务敏感读拒绝（v1 可常规）。图结构变更必须走冲突/诊断操作计划，工作台不直接执行。内置不可删基础高危集，管理员可追加（M1）。
