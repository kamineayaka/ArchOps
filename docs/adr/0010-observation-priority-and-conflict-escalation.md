# 观测优先；冲突按同一策展事实合并升级

在技术与经济可行时，观测全流程（探测、写入观测轨、比对策展、提醒）拥有最高优先，避免运维按过时实际位置排查。部分对象类型可降级为手动触发探测，但语义仍须写观测 → 比对 → 升级。已知悉可暂静音，但偏差变化或超时未对齐必须再提醒（N2）。渐行渐远以超时未对齐（G2）与已知悉期间实际偏差扩大（G3）判定。合并键为同一对象 + 同一关系类型（M1）；实际值从 B 变为 C 时不得并行 AB 与 AC，必须冲突升级为「策展 A、实际 C」，保留 A→B→C，且每次升级保留可审计提醒记录（H2）。
