# 诊断子图默认一跳双轨；失败则有限扩图最多三轮

诊断输入为策展子图与观测子图并行，外加冲突对齐表（B2）。第一轮默认范围为冲突合并键对象及其一跳邻居（S1）。诊断失败时可自问缺失对象/关系，并在策展图与观测图两侧查找，找到的并入对应子图（F1），允许部分未找到，合并后再诊。最多三轮：默认 1 + 扩图后再诊最多 2。任一终止即停并报失败与所需材料：所缺全部未找到；或已满三轮仍要扩图；或已满三轮仍无法判断。禁止无限扩图与猜测补全。
