# v1 用一张统一图，限定对象与关系种类

关系真相 v1 采用单一图模型（分层仅为视图），一等对象仅限：物理主机、数据库、Docker 容器、K8s 集群、K8s 节点。业务名只作标签。一等关系至少包括：主机间网络可达（含「可 SSH」这一策展事实）、运行于（容器→主机；数据库→主机或容器）、容器属于 K8s 集群、K8s 节点关联主机并属于集群。若改成多层独立图或把业务服务升为一等对象，查询、冲突比对与策展流程都要重做。
