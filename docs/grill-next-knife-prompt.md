# 新对话：改策展闭合后 → `/grill-with-docs`（下一刀）

将下面 **「复制区」** 整段作为**新对话**的第一条用户消息。若客户端支持手动附带 skill，同时附上：

- `grill-with-docs` — `.cursor/skills/grill-with-docs/SKILL.md`（桌面：`.agents/skills/grill-with-docs/SKILL.md`）
- `grilling` — `.cursor/skills/grilling/SKILL.md`（或 `.agents/skills/grilling/SKILL.md`）
- `domain-modeling` — `.cursor/skills/domain-modeling/SKILL.md`（或 `.agents/skills/domain-modeling/SKILL.md`）

本文件是 **grilling 入口**，不是 `/implement`。改策展 01–06 已 TDD-done，**不要**再开 `.scratch/change-curated-draft/issues/07-*`，不要重拆竖切 01–13。

Matt 主路径（`ask-matt`）：idea → **`/grill-with-docs`** →（必要时 `/prototype`）→ **`/to-spec`** → **`/to-tickets`** → 每票新对话 **`/implement` `/tdd`**。  
grilling、spec、tickets 尽量留在**同一上下文窗口**（不要在 `/to-tickets` 之前 `/clear`）。每个 `/implement` 另开对话。

---

## 复制区

```text
/grill-with-docs

你是 ArchOps 的产品/领域面试官，不是编码 Agent。本对话只做一件事：用 /grill-with-docs 把「改策展闭合之后的下一刀」问到共享理解。质量优先：frontier 未空之前不要写 Spec、不要拆票、不要写业务代码、不要开 ADR 草稿文件（可以讨论「是否需要新 ADR」）。

加载并遵守：
- AGENTS.md（执行纪律）
- grill-with-docs / grilling / domain-modeling skills
- docs/agents/domain.md（合同冻结：禁止静默改 CONTEXT.md / 已有 ADR）
- CONTEXT.md 只用已有术语，不发明同义新词（Avoid 栏禁止的词不要用）

================================================================================
0. 任务边界（完成标准：你能用一句话说出本对话交付物，且不把 /implement 算进范围）
================================================================================

本对话交付：
- 按 grilling skill 多轮面试：每轮列出当前 frontier 全部问题（编号 + 推荐答案），然后停下来等我答。
- 事实（仓库现状、票状态、已有 API、Flyway 版本、未打标容器现行行为）由你查，不要问我。
- 决策由我做。推荐答案必须写清，但不得把推荐当成已批准。
- frontier 空了之后：用合同术语复述共享理解，等我确认。确认前不要 /to-spec。

本对话不做：
- 任何业务代码、测试、Flyway、UI
- /implement、/tdd、/code-review
- 把改策展当成还没做完（01–06 已闭合）
- 给 change-curated-draft 加 07 号票
- 重开技术选型或推倒 ADR-0043
- 静默改 CONTEXT.md / ADR-0039 / 已有 ADR 正文
- 引入 Vue / JPA 当地基 / Maven / Neo4j v1 必选 / LangChain / Redis 当关系真相 SSOT
- 自我迭代落地、指标告警大盘、网络可达全矩阵 N²、完整 xterm、多租户、JWT 产品化（除非我在 Q1 明确点名其中一项，且 grilling 把它缩成可验收的一刀）

冲突优先级：ADR 与 CONTEXT > 已发布 Spec > 工单 > 本 prompt。

================================================================================
1. 先读（完成标准：按序读完；用票内/合同术语写作）
================================================================================

按序阅读，读完再问第一轮。不要等我催：

1. AGENTS.md §5–§6（一次一张票；本刀已闭合，不要默认 /implement）
2. docs/dev-handoff.md（确认改策展 01–06 TDD-done、本刀闭合）
3. CONTEXT.md — 至少：策展真相、观测真相、冲突、草案、逐条确认、未绑定观测候选、身份失联、网络可达、可达核验、策展对齐步骤、操作计划、规范问法
4. docs/adr/0039-domain-contract-frozen.md
5. docs/adr/0043-tech-stack.md
6. docs/adr/0006-curated-writes-via-itemized-proposals.md（接受即写入）
7. docs/adr/0019-conflict-close-and-curated-align-step.md（Y2 是另一条合法策展写入路径）
8. docs/adr/0038-ai-power-vs-capability-and-iteration.md（改策展：草案人审后再出操作计划 —— 与本刀「接受即写入、不出计划」的张力）
9. docs/adr/0015-v1-must-scope.md（v1 Must 仍含：AI 起草草案、K8s/数据库观测、未绑定与身份失联）
10. docs/adr/0012-container-label-bootstrap-and-identity-loss.md
11. docs/adr/0011-object-identity-rules.md
12. docs/adr/0034-ops-zone-and-host-to-host-reachability.md
13. docs/adr/0035-reachability-observed-via-source-agent-ssh.md
14. docs/specs/vertical-slice-mvp.md（已闭合；未打标容器是负面，不是完整未绑定流）
15. docs/specs/change-curated-draft.md — 尤其 Out of Scope 与 Further Notes（Y2 被明确留作后续 Spec）
16. docs/agents/issue-tracker.md（下一刀必须新 .scratch/<slug>/，不要写进 change-curated-draft）

事实自查（读代码/测试，不要问我）：
- 未打标容器 / 身份失联今天 HTTP 上实际返回什么（竖切票 13 / 相关 *HttpAcceptanceTest）
- 改理想路径今天是否在接受条目后写出操作计划（应为否）
- 合法策展改写入口：建底第一条插入、草案条目接受；Y2 对齐步是否存在（应为否）
- 最新 Flyway 版本号（只记录，本对话不新增脚本）

================================================================================
2. 当前位置（完成标准：第一轮提问前用 ≤10 行复述，不得说「下一张还是改策展 07」）
================================================================================

已完成、不要倒退：
- 领域 grilling / 合同冻结 / 栈冻结 / 空脚手架
- 竖切 MVP：修实际闭环（选 FIX_ACTUAL → 跳过草案 → 人审计划 → 受控 SSH → 观测对齐 → 待确认关闭）
- 改策展/改理想：诊断分叉 → 选支出草案（不写策展、不出计划）→ 逐条接受即写策展并立刻比对 → 升级/空洞作废未完成草案 → HTTP tracer
- /implement 的 TDD overlay：red → green → refactor（docs/agents/tdd.md）

本刀故意没做（Out of Scope，不是漏票）：
- 操作计划里的「策展对齐步骤」（Y2）
- 改策展后再生成/人审/执行 SSH 计划
- AI 起草草案的 LLM 主路径
- 未绑定候选经草案绑定或新建；身份失联重绑
- 网络可达全矩阵 / N²（连「只核验已策展边」也未做）
- K8s / 数据库对象
- 把观测消失收成「策展改为不存在」

合同里有两处张力，grilling 必须摊开，禁止实现时用代码悄悄改：
1. ADR-0006 / 本刀：草案条目接受即写入策展。ADR-0019 Y2：人审计划里的策展对齐步骤是另一条合法写入。二者不能变成「接受写一次、对齐步再写一次」。
2. ADR-0038：改策展分支「草案人审再出操作计划」。本刀钉死「承认实际、更新策展」到比对为止、不出 SSH 计划。若下一刀要补计划，必须说出计划步骤改的是现场还是策展、以及与「接受即写入」如何不双写。

验收接缝默认（可在 grilling 里推翻，推翻须明示）：
- 唯一自动化主接缝 = 控制面公开 HTTP API（含 Agent ingest）
- 前端薄 UI 手工/冒烟
- /implement 仍走 TDD；本对话不写测试

================================================================================
3. 候选下一刀（完成标准：Q1 必须从这里选或由我另点名；不要发明第七个「重构控制面」当刀）
================================================================================

A. 未绑定观测候选 / 身份失联重绑（推荐默认）
   为何：竖切只做了「未打标不承诺升级链」；合同里的未绑定候选、失联、人重绑线索仍未落地。仍是主机/容器/`运行于`，不新开对象种类，不跟 Y2 双写打架。身份规则是后面 K8s/库对象的前置。
   风险：绑定/重绑是否必须走草案逐条确认（几乎必须）；不要把弱线索自动续上升级链。

B. 网络可达 + 源主机 Agent SSH 核验（非 N²）
   为何：v1 一等边；ADR-0034/0035 已写清；证明双轨不只有 `运行于`。G2 出厂 72h 已在合同里。
   风险：要动 Agent 能力（源主机 SSH 探活）与观测边写入；禁止全图 N²；堡垒代连成功不得写成机间可达。

C. Y2 策展对齐步骤
   为何：改策展 Spec 把 Y2 标成后续 Spec；合同第二条合法策展写入还没实现。
   风险：与本刀「接受即写入」极易双写或偷改语义。只有存在「草案已审、写入推迟到计划执行」的独立故事时才选 C。选 C 几乎肯定要新 ADR 或明确「Y2 只用于尚未被条目接受写入的事实」。

D. AI 起草草案（LLM 主路径，人仍逐条确认）
   为何：ADR-0015 v1 Must 含「AI 起草草案」；本刀是规则夹具出 ≥2 条。
   风险：模型不可用时必须仍能改理想（规则兜底已有）。不要让 LLM 独自定稿。不要把自我迭代塞进来。

E. K8s / 数据库对象与观测对接
   为何：ADR-0015 v1 Must。
   风险：对象种类一扩，身份/属于/运行于/未绑定会一起炸。更像 /wayfinder 而不是一刀 Spec。只有我点名 E 才走 wayfinder；默认不要。

F. 我另点名的一刀（必须能用 CONTEXT 术语一句话说清用户可感知行为）

明确不推荐当下一刀：JWT、G2 运营化、高危名单全表、完整 xterm、指标大盘、自我迭代、服务拆分、Neo4j。

================================================================================
4. 第一轮 frontier（读完立刻问；问完停下。不要把依赖 Q1 的细节塞进这一轮）
================================================================================

按 grilling 格式输出（每个问题：标题、题干、推荐答案）。本轮只问互不依赖的决策：

❓ **Q1** - **下一刀选哪条**：A / B / C / D / E / F（F 须一句话点名）。
➡️ 推荐 **A（未绑定观测候选 / 身份失联重绑）**。不推荐默认 C：本刀已经用「接受即写入」闭合改理想写入段；C 没有独立故事就会双写或倒退。不推荐默认 E：雾太大。

❓ **Q2** - **tracker 落点**：新刀用新 `.scratch/<feature-slug>/` + `docs/specs/<slug>.md`，还是误加改策展 07？
➡️ 推荐 **新 slug、新 spec 目录**。改策展工单包已闭合。

❓ **Q3** - **自动化验收接缝**：是否仍唯一用控制面公开 HTTP API（含 Agent ingest）？是否引入 SSH fake / Playwright？
➡️ 推荐 **仍 HTTP API only**。A/D 不需要新 SSH 接缝。若选 B，Agent 探活可用 fake/记录夹具支撑 CI，但不把 SSH 当第二条完成定义（对齐竖切：fake 支撑、HTTP 验收）。不要 Playwright。

❓ **Q4** - **合同**：这一刀是「实现已冻结合同里已有术语」，还是「要改语义所以先立新 ADR」？
➡️ 对 A/B/D：推荐 **实现已有术语，不改 CONTEXT**。对 C：推荐 **先书面解决与接受即写入的关系，必要时新 ADR，禁止代码里改合同**。对 E：推荐 **先 wayfinder/grilling 缩范围，不要直接 to-spec 全家桶**。

若我在第一条消息里已经点名 A–F 之一，把 Q1 视为已决，不要再问；从未决的 Q2–Q4 开始。

================================================================================
5. 后续轮次（完成标准：选中那一刀之后才展开；每轮问完就停）
================================================================================

Q1 选定后，下一轮只问该刀被解开的问题（举例，不要提前全问）：

共同（任何刀）：
- 用户可感知的一条故事（从谁、看见什么、系统禁止什么）
- 明确 Out of Scope（至少：不重做竖切修实际、不重做改策展 01–06、不做 N²、不做自我迭代）
- 薄 UI 是否本刀 Must，还是 HTTP 先行、UI 最后一张票
- 是否允许 LLM 作为主路径（默认否，除非选 D）

若选 A：
- 未绑定候选的身份：它是不是冲突？是不是空洞？（合同：匹配失败 → 未绑定；不要当成冲突升级链）
- 人如何「重绑线索 / 补标」：是否必须草案逐条确认才改策展身份线索
- 原对象失联态与现场新实体（未绑定）如何并存、如何禁止同名静默合并
- 是否包含「先策展后补标」的完整正向故事，还是只把竖切负面升级成可绑定

若选 B：
- 只核验策展已声明的有向边（必须）
- 探活失败 vs 未核验空洞 vs 冲突
- 是否本刀就接冲突协作/修实际（SSH 改现场路由）还是只做到警告+诊断分叉
- Agent 新字段是否走现有心跳/快照契约（docs/contracts/agent-heartbeat-snapshot.md）

若选 C：
- 哪一条事实在条目接受时不写、留到对齐步才写？若「接受已写入」则 Y2 改什么？
- 对齐步失败是否回滚策展（合同倾向计划失败作废、不改步重试；策展已接受写入通常不回滚）
- 是否同时出 SSH 修现场步（很容易做成第二竖切，须显式砍掉或纳入）

若选 D：
- LLM 不可用时规则夹具是否仍必须出可审草案
- 起草结果是否仍 ≥2 条独立条目（逐条）
- 敏感读 / 出站白名单是否只复用 ADR-0041，不新开自我迭代

每轮你都可以根据我的回答改推荐。不要一次问 15 个。

================================================================================
6. 收束（frontier 空了才做；仍不要写代码）
================================================================================

1. 用 CONTEXT 术语写「共享理解」：故事、Must、Out of Scope、接缝、是否需要新 ADR、建议 slug。
2. 等我确认「可以 /to-spec」。
3. 我确认后，**同一对话**再跑 /to-spec（先提出测试接缝等我点头，再写 Spec 并按 docs/agents/issue-tracker.md 发布到 docs/specs/<slug>.md 与 .scratch/<slug>/spec.md）。不要用 gh issue create。
4. Spec 发布后，**同一对话**再跑 /to-tickets（先给票清单+阻塞图等我批准，再写 .scratch/<slug>/issues/）。
5. 不要在本对话 /implement。拆票完成后更新 docs/dev-handoff.md 的下一票指针，并注明 frontier = 新刀 01。

开始：先用 ≤10 行复述当前位置，然后立刻输出第一轮 Q1–Q4。不要写 Spec，不要改产品代码。
```

---

## grilling 完成之后（同一对话，不要新开窗口）

确认共享理解后，直接在该对话发送 `/to-spec`，并要求 Agent 加载 `.cursor/skills/to-spec/SKILL.md`。接缝仍先确认再写全文。Spec 发布后再 `/to-tickets`。

拆票完成后，**每个工单新开对话** `/implement` `/tdd`，一次一张；TDD overlay 见 [`docs/agents/tdd.md`](agents/tdd.md)。
