{
  "name": "ArchOps Cloud Agent",
  "user": "ubuntu",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "bash scripts/cloud-install.sh",
  "start": "bash scripts/cloud-start.sh",
  "ports": [
    {
      "name": "backend API",
      "port": 8080
    },
    {
      "name": "frontend Vite",
      "port": 5173
    }
  ],
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && ARCHOPS_CREDENTIALS_ENCRYPTION_KEY_BASE64=YXJjaG9wcy1maXh0dXJlLWFlcy0yNTYta2V5ISEhISE= ./gradlew bootRun --no-daemon",
      "description": "Spring Boot control plane on :8080 (health: GET /api/health). Needs Compose Postgres+Redis from start. Injects an explicit fixture encryption key (not a Compose default)."
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host 0.0.0.0 --port 5173",
      "description": "Vite + React + Ant Design; proxies /api to http://127.0.0.1:8080"
    }
  ]
}
