---
description: 远程验证机 kamiserver（部署/冒烟测试目标，非开发机）
alwaysApply: true
---

# 远程验证：kamiserver

本机可能无 Docker。**部署验证与冒烟测试默认在 kamiserver**。

Neo4j 为必选依赖；主机建议 **≥4–8 GiB**。

## 目标机

| 项 | 值 |
|---|---|
| SSH | `ssh kamiserver` |
| 规格 | 建议 ≥4C / ≥8G |
| 角色 | **仅**测试与部署验证，禁止当日常开发机 |
| 应用路径 | 按部署时选择（常见 `/opt/archops`） |
| 访问 | 以主机 IP / Tailscale IP 为准 |

## 验证流程

在仓库 `deploy/compose` 下使用标准 Compose：

```bash
cd deploy/compose
cp .env.example .env
# CORS_ALLOWED_ORIGINS=http://<kami-ip>,http://localhost
# NEO4J_PASSWORD=archopsneo4j

docker compose up -d --build

curl -fsS http://<kami-ip>/actuator/health/liveness
```

用户说「部署 / 验证 / 上机测一下」时，默认对 **kamiserver** 执行上述路径。
