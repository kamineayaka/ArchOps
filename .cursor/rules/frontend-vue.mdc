---
description: 前端 Vue3/Naive UI 编码约定（对齐 AssetsView / api/assets）
globs: frontend/**/*.{vue,ts}
alwaysApply: false
---

# 前端 Vue 约定

`Vue 3` + `<script setup lang="ts">` + Naive UI + Pinia。跟现有 View/API 对齐，不引入新 UI 框架。**已取消 vue-i18n**，禁止再加回。

## 目录

| 路径 | 用途 |
|---|---|
| `src/api/` | HTTP 封装（唯一出口） |
| `src/views/` | 路由页面 |
| `src/components/` | 可复用 UI |
| `src/composables/` | 组合逻辑 |
| `src/stores/` | Pinia |
| `src/messages.ts` | 文案字典 + `t()`（单语，非 i18n） |
| `src/assetTypes/` | 资产类型扩展 |

路径别名用 `@/`。无 `src/locales/`。

## API 层

- 用 `src/api/client.ts`（已带 Bearer / 401 踢登录），禁止另建 axios 实例
- 函数返回 `data`（`ApiResponse<T>`），类型放同文件或 `api/types.ts`
- 错误展示：`apiErrorMessage(err, t('...'))` + `useMessage()`

```ts
// ✅
export async function listAssets() {
  const { data } = await client.get<ApiResponse<Asset[]>>('/api/assets')
  return data
}

// ❌ fetch 直调 / 页面内拼 URL / 忽略 ApiResponse 包装
```

## 页面

- 结构：`PageHeader` + Naive 组件；列表优先 `NDataTable` + `computed` columns
- 文案：`import { t } from '@/messages'`，新键写进 `messages.ts`；禁止 `vue-i18n` / `useI18n` / `locales/`
- 状态：`ref` / `computed`；跨页再用 Pinia
- 异步：`loading` ref；`try/catch` 后 `message.error(apiErrorMessage(...))`

## 资产相关

新资产能力走 `assetTypes` 注册表，不要在 View 里写死 `kind` 分支（参考 `AssetsView` + `getAssetType`）。

## 样板

`api/auth.ts`、`api/assets.ts`、`api/client.ts`、`messages.ts`、`utils/apiError.ts`、`views/AssetsView.vue`
