---
description: 后端 Java/Spring 编码约定（绿场；持久化以 MyBatis-Plus 为准）
globs: backend/**/*.java
alwaysApply: false
---

# 后端 Java 约定

包根 `com.archops.<模块>`。构建 **Gradle**；持久化 **MyBatis-Plus**（ADR-0043）。不以 JPA 为新代码默认。云端 Agent 另见根目录 `AGENTS.md`。

## 分层

| 层 | 职责 | 禁止 |
|---|---|---|
| `controller` | HTTP、鉴权、校验、调 Service | 业务逻辑、直接 Mapper |
| `service` | 事务、领域规则 | 拼 HTTP 细节乱抛 |
| `dto` | `record` 请求/响应 | Entity/DO 直接暴露 API |
| `mapper` | MyBatis-Plus Mapper + SQL | 业务编排 |
| `domain` / DO | 表映射对象 | 当 API DTO 用 |

构造器注入；写操作事务边界在 service。

## API

- `/api/...` + 统一响应体
- 业务错误用 `BusinessException`（或等价）
- 临时身份：`X-ArchOps-User-Id` → 后续 JWT；会话可落 Redis

## 其他

- Flyway 只增不改历史
- Redis：队列/锁/会话/缓存；不作真相 SSOT
- SSH：MINA SSHD；计划互斥用分布式锁
- AI：WebClient + ADR-0041；无 LangChain 主干
- 图库 Later；v1 邻域用 SQL/CTE
- `/implement` 的 HTTP 验收循环见 `docs/agents/tdd.md`（本文件只管分层）
