# Copy to .env next to compose.yaml if needed (Compose auto-loads).
POSTGRES_DB=archops
POSTGRES_USER=archops
POSTGRES_PASSWORD=archops
POSTGRES_PORT=5432
REDIS_PORT=6379
REDIS_PASSWORD=
ARCHOPS_PORT=8080
ARCHOPS_IMAGE=archops:latest
