# ArchOps AI Platform - Docker Compose (production-ready single-node)

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: archops-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-archops}
      POSTGRES_USER: ${POSTGRES_USER:-archops}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-archops}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-archops} -d ${POSTGRES_DB:-archops}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: archops-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ../../backend
      dockerfile: Dockerfile
      # Prefer locally prefetched layers; buildx often ignores dockerd registry-mirrors.
      pull: false
      args:
        # Optional: https://maven.aliyun.com/repository/public
        MAVEN_MIRROR: ${MAVEN_MIRROR:-}
    container_name: archops-backend
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: compose
      JAVA_OPTS: ${JAVA_OPTS:--XX:MaxRAMPercentage=75.0}
      JWT_SECRET: ${JWT_SECRET:-}
      CREDENTIALS_MASTER_KEY: ${CREDENTIALS_MASTER_KEY:-}
      ARCHOPS_SECRETS_PATH: /data/archops/secrets.properties
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-archops}
      POSTGRES_USER: ${POSTGRES_USER:-archops}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-archops}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost}
      AI_DEFAULT_PROVIDER: ${AI_DEFAULT_PROVIDER:-openai-compat}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-qwen2.5:14b}
      RAG_ENABLED: ${RAG_ENABLED:-true}
      RAG_PROVIDER: ${RAG_PROVIDER:-openai-compat}
      RAG_EMBEDDING_MODEL: ${RAG_EMBEDDING_MODEL:-text-embedding-3-small}
      RAG_EMBEDDING_DIMENSIONS: ${RAG_EMBEDDING_DIMENSIONS:-1536}
      RAG_TOP_K: ${RAG_TOP_K:-5}
      RAG_MIN_SIMILARITY: ${RAG_MIN_SIMILARITY:-0.35}
      ARCH_AUTO_MERGE_ENABLED: ${ARCH_AUTO_MERGE_ENABLED:-false}
      # Graph off by default. NEO4J_* injected only via compose.graph.yaml (--profile graph).
      ARCHOPS_GRAPH_ENABLED: ${ARCHOPS_GRAPH_ENABLED:-false}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      # When --profile graph is off, Compose skips this dependency (required: false).
      neo4j:
        condition: service_healthy
        required: false
    # No curl in runtime image: TCP+HTTP via bash /dev/tcp (Ubuntu JRE has bash).
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -c 'exec 3<>/dev/tcp/127.0.0.1/8080 && printf \"GET /actuator/health/liveness HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3 && timeout 4 cat <&3 | grep -qi UP'",
        ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 90s
    stop_grace_period: 30s
    volumes:
      - archops_secrets:/data/archops

  neo4j:
    image: neo4j:5.26-community
    container_name: archops-neo4j
    restart: unless-stopped
    profiles: ["graph"]
    environment:
      NEO4J_AUTH: ${NEO4J_USERNAME:-neo4j}/${NEO4J_PASSWORD:-archopsneo4j}
      NEO4J_dbms_security_auth__minimum__password__length: "8"
      NEO4J_server_memory_heap_initial__size: 256m
      NEO4J_server_memory_heap_max__size: 256m
      NEO4J_server_memory_pagecache_size: 64m
    ports:
      - "${NEO4J_HTTP_PORT:-7474}:7474"
      - "${NEO4J_BOLT_PORT:-7687}:7687"
    volumes:
      - neo4j_data:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7474 || exit 1"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 60s

  frontend:
    build:
      context: ../../frontend
      dockerfile: Dockerfile
      pull: false
      args:
        # Optional: https://registry.npmmirror.com
        NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmjs.org}
    container_name: archops-frontend
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-80}:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  archops_secrets:
  neo4j_data:
